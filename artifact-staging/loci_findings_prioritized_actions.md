# Loci findings: prioritized actions for PR readiness

Date: 2026-09-20  
Scope reviewed: online relevance/fact-check artifacts, Wave24 non-fuzzing expansions, Wave25 gap analyses, Wave23 FDE simulation.

## Prioritized actions (evidence-bounded)

| Priority | Action | Why now | Evidence anchors | Current status | Confidence | Deterministic next test |
|---|---|---|---|---|---|---|
| 1 | Prove or refute unauthorized `core_values` read/write from a non-privileged app. | This is the clearest path from local access to identity/token abuse. | `wave24_identity_chain_expansion.md` (provider exported + normal permission; read/write paths), `online_loci_factcheck_matrix.md` (local data-path claims corroborated). | Corroborated risk, runtime boundary not fully closed. | High | Send controlled query/insert/update to `content://.../core_values` from non-privileged app; record allow/deny + logs. |
| 2 | Validate OTA policy invariants at API boundary (version floor, signer-domain binding, auth-mode parity). | Client-side mechanics exist; backend policy enforcement is still unproven. | `wave24_ota_governance_expansion.md` (unverified backend controls), `wave25_runtime_boundary_gaps.md` (updater boundary candidates). | Unverified | High | Execute API matrix tests for `/systemPatch` using valid/invalid version and signer combinations across bearer and fallback auth. |
| 3 | Close physical boot-trust ambiguity with live boot attestation evidence. | Physical trust state drives severity of multiple chains (FDE, persistence, rollback). | `wave23_unlocked_bootloader_fde_bypass_simulation.md` (weak trust posture + bounded claims), `wave24_physical_chain_expansion.md` (boot tamper chain likely), `online_loci_factcheck_matrix.md` (locked-device claim contradicted). | Corroborated that trust is weak; full live proof incomplete. | High | Collect `fastboot` lock/verity/AVB outputs and boot warnings on target hardware; compare against expected secure baseline. |
| 4 | Test reset/persistence behavior for `/persist` and Auth0 artifacts. | Recovery trust claims remain uncertain without before/after reset evidence. | `wave24_physical_chain_expansion.md` (likely persistence by omission), `wave25_privacy_retention_gaps.md` (persisted Auth0 artifacts + reset residue concern). | Unverified | Medium | Capture hashes/listing for `/persist/flock/auth0/*` pre/post reset and factory reset; confirm retention or purge behavior. |
| 5 | Verify retention cleanup coverage for `media-db` and `kernel-oops` artifacts. | Privacy and minimization posture may be overstated if sensitive artifacts outlive retention window. | `wave25_privacy_retention_gaps.md` (cleanup scope narrower than artifact generation). | Unverified | High | Generate artifacts, invoke partition cleanup trigger, and verify whether `media-db*` and `kernel-oops/*` are removed. |
| 6 | Validate exploitability boundary of exported diagnostics (`DatabaseExportReceiver`) under SELinux. | Exposure is proven at export step, but third-party readability controls are still open. | `wave24_physical_chain_expansion.md` (DB export proven), `wave25_runtime_boundary_gaps.md` + `wave25_privacy_retention_gaps.md` (readability unverified). | Corroborated export behavior; downstream readability unverified. | Medium | Trigger export, attempt read from untrusted app UID, and capture SELinux allow/deny evidence. |
| 7 | Validate `reaperd` socket command effects and privilege boundary. | Root-level control surface may materially change impact if commands are actionable. | `wave24_physical_chain_expansion.md` (world-writable socket + likely control-plane risk), `wave25_runtime_boundary_gaps.md` (runtime boundary closure still needed). | Unverified side-effect severity | Medium | Run controlled socket command sequence and log actual side effects (service control, reboot/relaunch behavior, access checks). |
| 8 | Resolve secret lifecycle and OTA trust-path governance evidence gaps. | Persisted machine credentials and plain-HTTP OTA references require closure for PR credibility. | `wave25_supplychain_secret_gaps.md` (persisted Auth0 secret, lifecycle evidence gaps, OTA trust-path concern). | Corroborated presence; lifecycle/policy controls unverified. | High | Confirm active secret rotation/revocation records and verify update authenticity controls (HTTPS + signature/trust-anchor enforcement). |

## PR-readiness framing

- **Safe to state now (corroborated):** weak boot trust indicators, provider/data-path exposure risk, exported diagnostics flow, persisted sensitive credential artifacts.
- **Must stay conditional (unverified):** direct universal `/data` extraction, backend policy enforcement guarantees, SELinux readability outcomes, reset purge guarantees.
- **Explicitly contradicted in current evidence set:** “device is locked + AVB2 enforced,” and “unlocked bootloader alone proves complete direct `/data` bypass.”
